---
title: "Github Pages 배포하기 with gh-pages"
date: "240605"
tags: ["GithubPages"]
---

0. 이곳을 [https://rxolve-github-io.vercel.app](https://rxolve-github-io.vercel.app) 에서 [https://rxolve.github.io](https://rxolve.github.io) 로 변경하는 작업을 진행했습니다.
1. next.config.mjs 에 아래와 같이 설정을 추가합니다.

   ```javascript
     output: 'export',
     images: {
       unoptimized: true,
     },

   ```

2. npm run build 를 해줍니다. out 폴더 안에 산출물이 생깁니다. out 폴더를 gitignore 에 추가합니다.
3. gh-pages 를 설치합니다.
   > npm install --save-dev gh-pages
4. package.json 에 아래 스크립트를 추가합니다.
   ```json
   "scripts": {
    "build": "next build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d out",
   }
   ```
5. npm run deploy 를 실행합니다.
6. 깃헙 rxolve.github.io 프로젝트의 Settings > Pages 에서 Source 를 gh-pages 브랜치로 변경합니다.
7. https://rxolve.github.io 에 접속하면 배포된 페이지를 확인할 수 있었습니다. 하지만 Pico CSS가 적용되지 않아 매우 볼품 없어 슬픕니다.
   - 원인은 \_next/static 폴더의 파일들이 404 에러를 뱉어내고 있습니다.
   - 해결은 내일로 미뤄봅니다. 돌아오세요, 피코 선생님.
